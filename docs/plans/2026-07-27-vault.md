# Vault Implementation Plan

> **Status: fully implemented.** All 18 tasks are in `main`. Two deviations from
> what is written below, both deliberate: the whole codebase is in English rather
> than Spanish, and Task 18 targets the existing Docker/Caddy deployment instead
> of adding an nginx configuration that would contradict it. Two gaps found while
> implementing were fixed and are not described here: the setup endpoint now
> authenticates the session (otherwise the first passkey could never be enrolled)
> and `GET /api/account/me` was added so the SPA can rebuild its state after a
> page reload.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir un gestor personal de contraseñas y claves autoalojado, de usuario único, en el que el servidor nunca puede descifrar lo que almacena.

**Architecture:** Laravel 13 actúa exclusivamente como almacén de blobs opacos y como autenticador (auth hash + WebAuthn). Toda la criptografía ocurre en el navegador sobre WebCrypto: la master password deriva una master key, de ella salen por HKDF un auth hash (viaja al servidor) y una wrapping key (nunca sale), y esta última envuelve una Vault Key aleatoria que es la que cifra cada ítem con AES-256-GCM. El frontend es una SPA Vue 3.5 que mantiene la Vault Key solo en memoria.

**Tech Stack:** PHP 8.4 · Laravel 13.22 · SQLite · PHPUnit 12.5 · Vue 3.5 + TypeScript · Pinia · vue-router · Tailwind v4 · Vite 8 · Vitest · `web-auth/webauthn-lib` · `@simplewebauthn/browser`

**Spec:** `docs/specs/2026-07-27-vault-design.html`

## Global Constraints

- El servidor **nunca** recibe, almacena ni registra una master password, una Vault Key desenvuelta ni un plaintext. Cualquier tarea que lo viole está mal implementada.
- Aleatoriedad **siempre** desde `crypto.getRandomValues` (cliente) o `random_bytes` (servidor). `Math.random` y `rand()` están prohibidos en todo el proyecto.
- KDF: PBKDF2-SHA256, **600000** iteraciones, salt de **16 bytes**, salida de **256 bits**.
- Cifrado simétrico: **AES-256-GCM**, IV aleatorio de **12 bytes** por operación, jamás reutilizado.
- HKDF-SHA256 con `info` = `vault:auth:v1` para el auth hash y `vault:wrap:v1` para la wrapping key.
- La Vault Key vive en una variable de módulo, **fuera** del estado reactivo de Pinia. Nunca en `localStorage`, `sessionStorage` ni IndexedDB.
- El fichero SQLite y los backups no se versionan nunca (ya cubierto en `.gitignore`).
- Idioma de la interfaz: español. Idioma del código, comentarios y commits: español.
- Commits al final de cada tarea, sin excepción.

---

### Task 1: Base del frontend y arranque de Vitest

Deja el proyecto capaz de compilar TypeScript, montar Vue y ejecutar tests de unidad. Sin esto ninguna tarea posterior puede probarse.

**Files:**
- Modify: `package.json`
- Create: `tsconfig.json`
- Modify: `vite.config.js` → renombrar a `vite.config.ts`
- Create: `vitest.config.ts`
- Create: `resources/js/app.ts`
- Create: `resources/js/crypto/base64.ts`
- Test: `resources/js/crypto/base64.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces: `bytesToBase64(bytes: Uint8Array): string` y `base64ToBytes(b64: string): Uint8Array` desde `resources/js/crypto/base64.ts`. Las usan todas las tareas de criptografía.

- [ ] **Step 1: Instalar dependencias**

```bash
cd C:/laragon/www/vault
npm install vue@^3.5 pinia vue-router @simplewebauthn/browser
npm install -D typescript vue-tsc @vitejs/plugin-vue vitest
```

- [ ] **Step 2: Crear `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "jsx": "preserve",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "skipLibCheck": true,
    "noEmit": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "paths": { "@/*": ["./resources/js/*"] },
    "baseUrl": "."
  },
  "include": ["resources/js/**/*.ts", "resources/js/**/*.vue"]
}
```

- [ ] **Step 3: Renombrar y reescribir la configuración de Vite**

```bash
git mv vite.config.js vite.config.ts
```

`vite.config.ts`:

```ts
import { defineConfig } from 'vite'
import laravel from 'laravel-vite-plugin'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [
    laravel({ input: ['resources/css/app.css', 'resources/js/app.ts'], refresh: true }),
    vue(),
    tailwindcss(),
  ],
  resolve: {
    alias: { '@': fileURLToPath(new URL('./resources/js', import.meta.url)) },
  },
})
```

- [ ] **Step 4: Crear `vitest.config.ts`**

El entorno es `node` a propósito: Node 24 expone WebCrypto de forma nativa en `globalThis.crypto`, que es exactamente la API que usa el navegador. Probamos la criptografía real, no un mock.

```ts
import { defineConfig } from 'vitest/config'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  test: { environment: 'node', include: ['resources/js/**/*.test.ts'] },
  resolve: {
    alias: { '@': fileURLToPath(new URL('./resources/js', import.meta.url)) },
  },
})
```

- [ ] **Step 5: Añadir scripts a `package.json`**

Dentro de `"scripts"`:

```json
"test": "vitest run",
"test:watch": "vitest",
"typecheck": "vue-tsc --noEmit"
```

- [ ] **Step 6: Escribir el test que falla**

`resources/js/crypto/base64.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { bytesToBase64, base64ToBytes } from './base64'

describe('base64', () => {
  it('convierte bytes conocidos a base64', () => {
    expect(bytesToBase64(new Uint8Array([72, 111, 108, 97]))).toBe('SG9sYQ==')
  })

  it('descodifica base64 conocido a bytes', () => {
    expect(Array.from(base64ToBytes('SG9sYQ=='))).toEqual([72, 111, 108, 97])
  })

  it('sobrevive a la ida y vuelta con bytes binarios arbitrarios', () => {
    const original = crypto.getRandomValues(new Uint8Array(64))
    expect(Array.from(base64ToBytes(bytesToBase64(original)))).toEqual(Array.from(original))
  })

  it('maneja el array vacío', () => {
    expect(bytesToBase64(new Uint8Array(0))).toBe('')
    expect(base64ToBytes('').length).toBe(0)
  })
})
```

- [ ] **Step 7: Ejecutar el test y comprobar que falla**

Run: `npm test`
Expected: FAIL — no existe el módulo `./base64`.

- [ ] **Step 8: Implementar `resources/js/crypto/base64.ts`**

```ts
/** Conversión entre Uint8Array y base64, sin dependencias externas. */

export function bytesToBase64(bytes: Uint8Array): string {
  let binario = ''
  for (const byte of bytes) binario += String.fromCharCode(byte)
  return btoa(binario)
}

export function base64ToBytes(b64: string): Uint8Array {
  const binario = atob(b64)
  const bytes = new Uint8Array(binario.length)
  for (let i = 0; i < binario.length; i++) bytes[i] = binario.charCodeAt(i)
  return bytes
}
```

- [ ] **Step 9: Ejecutar el test y comprobar que pasa**

Run: `npm test`
Expected: PASS, 4 tests.

- [ ] **Step 10: Crear `resources/js/app.ts` mínimo**

```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'

const app = createApp({ template: '<div>Vault</div>' })
app.use(createPinia())
app.mount('#app')
```

- [ ] **Step 11: Verificar que el build funciona**

Run: `npm run build`
Expected: termina sin errores y genera `public/build/`.

- [ ] **Step 12: Commit**

```bash
git add -A
git commit -m "Configura Vue, TypeScript y Vitest; añade utilidades base64"
```

---

### Task 2: Derivación de claves (KDF)

El corazón criptográfico. Una master password y un salt producen una master key; de ella salen dos subclaves independientes.

**Files:**
- Create: `resources/js/crypto/kdf.ts`
- Test: `resources/js/crypto/kdf.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `interface KdfParams { algo: 'pbkdf2-sha256'; iterations: number; salt: Uint8Array }`
  - `deriveMasterKey(password: string, params: KdfParams): Promise<Uint8Array>` → 32 bytes
  - `deriveAuthHash(masterKey: Uint8Array): Promise<Uint8Array>` → 32 bytes
  - `deriveWrappingKey(masterKey: Uint8Array): Promise<Uint8Array>` → 32 bytes
  - `generateSalt(): Uint8Array` → 16 bytes
  - `const DEFAULT_ITERATIONS = 600000`

- [ ] **Step 1: Confirmar el vector de prueba oficial antes de fijarlo**

No fijes un valor esperado sin comprobarlo. Ejecuta:

```bash
node -e "const c=require('crypto');console.log(c.pbkdf2Sync('password','salt',1,32,'sha256').toString('hex'))"
```

Anota la salida. Debe ser el vector estándar de PBKDF2-HMAC-SHA256 (`password`/`salt`/1 iteración/32 bytes). Úsalo literalmente en el test del paso siguiente. Si más adelante el test falla, el error está en la implementación, no en el vector.

- [ ] **Step 2: Escribir el test que falla**

`resources/js/crypto/kdf.test.ts` — sustituye `VECTOR_OFICIAL` por la salida del paso 1:

```ts
import { describe, it, expect } from 'vitest'
import {
  deriveMasterKey, deriveAuthHash, deriveWrappingKey,
  generateSalt, DEFAULT_ITERATIONS, type KdfParams,
} from './kdf'

const hex = (b: Uint8Array) => Array.from(b).map(x => x.toString(16).padStart(2, '0')).join('')
const enc = new TextEncoder()

describe('deriveMasterKey', () => {
  it('reproduce el vector estándar de PBKDF2-HMAC-SHA256', async () => {
    const clave = await deriveMasterKey('password', {
      algo: 'pbkdf2-sha256', iterations: 1, salt: enc.encode('salt'),
    })
    expect(hex(clave)).toBe('VECTOR_OFICIAL')
  })

  it('es determinista: misma password y mismo salt dan la misma clave', async () => {
    const params: KdfParams = { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() }
    const a = await deriveMasterKey('correcta', params)
    const b = await deriveMasterKey('correcta', params)
    expect(hex(a)).toBe(hex(b))
  })

  it('un salt distinto produce una clave distinta con la misma password', async () => {
    const a = await deriveMasterKey('correcta', { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() })
    const b = await deriveMasterKey('correcta', { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() })
    expect(hex(a)).not.toBe(hex(b))
  })

  it('una password distinta produce una clave distinta con el mismo salt', async () => {
    const params: KdfParams = { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() }
    const a = await deriveMasterKey('correcta', params)
    const b = await deriveMasterKey('incorrecta', params)
    expect(hex(a)).not.toBe(hex(b))
  })

  it('devuelve 32 bytes', async () => {
    const clave = await deriveMasterKey('x', { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() })
    expect(clave.length).toBe(32)
  })
})

describe('subclaves', () => {
  it('el auth hash y la wrapping key son distintos entre sí', async () => {
    const mk = await deriveMasterKey('x', { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() })
    expect(hex(await deriveAuthHash(mk))).not.toBe(hex(await deriveWrappingKey(mk)))
  })

  it('cada subclave es determinista respecto a la master key', async () => {
    const mk = await deriveMasterKey('x', { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() })
    expect(hex(await deriveAuthHash(mk))).toBe(hex(await deriveAuthHash(mk)))
    expect(hex(await deriveWrappingKey(mk))).toBe(hex(await deriveWrappingKey(mk)))
  })

  it('ambas devuelven 32 bytes', async () => {
    const mk = await deriveMasterKey('x', { algo: 'pbkdf2-sha256', iterations: 1000, salt: generateSalt() })
    expect((await deriveAuthHash(mk)).length).toBe(32)
    expect((await deriveWrappingKey(mk)).length).toBe(32)
  })
})

describe('generateSalt', () => {
  it('devuelve 16 bytes distintos en cada llamada', () => {
    const a = generateSalt(), b = generateSalt()
    expect(a.length).toBe(16)
    expect(hex(a)).not.toBe(hex(b))
  })
})

describe('parámetros por defecto', () => {
  it('usa 600000 iteraciones', () => {
    expect(DEFAULT_ITERATIONS).toBe(600000)
  })
})
```

- [ ] **Step 3: Ejecutar el test y comprobar que falla**

Run: `npm test -- kdf`
Expected: FAIL — no existe el módulo `./kdf`.

- [ ] **Step 4: Implementar `resources/js/crypto/kdf.ts`**

```ts
/**
 * Derivación de claves a partir de la master password.
 *
 * La master key nunca sale de aquí sin pasar por HKDF: el auth hash que viaja
 * al servidor y la wrapping key que descifra la Vault Key son subclaves
 * computacionalmente independientes. Conocer una no ayuda a obtener la otra.
 */

export const DEFAULT_ITERATIONS = 600000
const SALT_BYTES = 16
const INFO_AUTH = 'vault:auth:v1'
const INFO_WRAP = 'vault:wrap:v1'

const enc = new TextEncoder()

export interface KdfParams {
  algo: 'pbkdf2-sha256'
  iterations: number
  salt: Uint8Array
}

export function generateSalt(): Uint8Array {
  return crypto.getRandomValues(new Uint8Array(SALT_BYTES))
}

export async function deriveMasterKey(password: string, params: KdfParams): Promise<Uint8Array> {
  const base = await crypto.subtle.importKey(
    'raw', enc.encode(password), 'PBKDF2', false, ['deriveBits'],
  )
  const bits = await crypto.subtle.deriveBits(
    { name: 'PBKDF2', salt: params.salt, iterations: params.iterations, hash: 'SHA-256' },
    base, 256,
  )
  return new Uint8Array(bits)
}

async function derivarSubclave(masterKey: Uint8Array, info: string): Promise<Uint8Array> {
  const base = await crypto.subtle.importKey('raw', masterKey, 'HKDF', false, ['deriveBits'])
  const bits = await crypto.subtle.deriveBits(
    { name: 'HKDF', hash: 'SHA-256', salt: new Uint8Array(0), info: enc.encode(info) },
    base, 256,
  )
  return new Uint8Array(bits)
}

export function deriveAuthHash(masterKey: Uint8Array): Promise<Uint8Array> {
  return derivarSubclave(masterKey, INFO_AUTH)
}

export function deriveWrappingKey(masterKey: Uint8Array): Promise<Uint8Array> {
  return derivarSubclave(masterKey, INFO_WRAP)
}
```

- [ ] **Step 5: Ejecutar el test y comprobar que pasa**

Run: `npm test -- kdf`
Expected: PASS, 10 tests.

- [ ] **Step 6: Commit**

```bash
git add resources/js/crypto/kdf.ts resources/js/crypto/kdf.test.ts
git commit -m "Añade derivación de master key y subclaves con PBKDF2 y HKDF"
```

---

### Task 3: Cifrado simétrico y Vault Key

**Files:**
- Create: `resources/js/crypto/aes.ts`
- Create: `resources/js/crypto/vault-key.ts`
- Test: `resources/js/crypto/aes.test.ts`
- Test: `resources/js/crypto/vault-key.test.ts`

**Interfaces:**
- Consumes: `bytesToBase64`, `base64ToBytes` de `@/crypto/base64`.
- Produces:
  - `interface Cifrado { ciphertext: string; iv: string }` (ambos en base64)
  - `encryptBytes(key: Uint8Array, datos: Uint8Array): Promise<Cifrado>`
  - `decryptBytes(key: Uint8Array, cifrado: Cifrado): Promise<Uint8Array>`
  - `generateVaultKey(): Uint8Array` → 32 bytes
  - `wrapVaultKey(wrappingKey: Uint8Array, vaultKey: Uint8Array): Promise<Cifrado>`
  - `unwrapVaultKey(wrappingKey: Uint8Array, envuelta: Cifrado): Promise<Uint8Array>`

- [ ] **Step 1: Escribir el test de AES que falla**

`resources/js/crypto/aes.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { encryptBytes, decryptBytes } from './aes'
import { base64ToBytes, bytesToBase64 } from './base64'

const clave = () => crypto.getRandomValues(new Uint8Array(32))
const enc = new TextEncoder()
const dec = new TextDecoder()

describe('AES-256-GCM', () => {
  it('descifra lo que cifra', async () => {
    const k = clave()
    const original = enc.encode('contraseña muy secreta')
    const resultado = await decryptBytes(k, await encryptBytes(k, original))
    expect(dec.decode(resultado)).toBe('contraseña muy secreta')
  })

  it('usa un IV de 12 bytes', async () => {
    const { iv } = await encryptBytes(clave(), enc.encode('x'))
    expect(base64ToBytes(iv).length).toBe(12)
  })

  it('nunca repite el IV entre escrituras', async () => {
    const k = clave()
    const ivs = new Set<string>()
    for (let i = 0; i < 200; i++) ivs.add((await encryptBytes(k, enc.encode('x'))).iv)
    expect(ivs.size).toBe(200)
  })

  it('produce ciphertext distinto para el mismo plaintext', async () => {
    const k = clave()
    const a = await encryptBytes(k, enc.encode('igual'))
    const b = await encryptBytes(k, enc.encode('igual'))
    expect(a.ciphertext).not.toBe(b.ciphertext)
  })

  it('falla si se altera un byte del ciphertext', async () => {
    const k = clave()
    const cifrado = await encryptBytes(k, enc.encode('íntegro'))
    const bytes = base64ToBytes(cifrado.ciphertext)
    bytes[0] ^= 0xff
    await expect(
      decryptBytes(k, { ciphertext: bytesToBase64(bytes), iv: cifrado.iv }),
    ).rejects.toThrow()
  })

  it('falla si se altera el IV', async () => {
    const k = clave()
    const cifrado = await encryptBytes(k, enc.encode('íntegro'))
    const iv = base64ToBytes(cifrado.iv)
    iv[0] ^= 0xff
    await expect(
      decryptBytes(k, { ciphertext: cifrado.ciphertext, iv: bytesToBase64(iv) }),
    ).rejects.toThrow()
  })

  it('falla con una clave incorrecta', async () => {
    const cifrado = await encryptBytes(clave(), enc.encode('secreto'))
    await expect(decryptBytes(clave(), cifrado)).rejects.toThrow()
  })
})
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `npm test -- aes`
Expected: FAIL — no existe `./aes`.

- [ ] **Step 3: Implementar `resources/js/crypto/aes.ts`**

```ts
import { bytesToBase64, base64ToBytes } from './base64'

const IV_BYTES = 12

export interface Cifrado {
  ciphertext: string
  iv: string
}

async function importar(key: Uint8Array, uso: KeyUsage[]): Promise<CryptoKey> {
  return crypto.subtle.importKey('raw', key, { name: 'AES-GCM' }, false, uso)
}

export async function encryptBytes(key: Uint8Array, datos: Uint8Array): Promise<Cifrado> {
  const iv = crypto.getRandomValues(new Uint8Array(IV_BYTES))
  const cifrado = await crypto.subtle.encrypt(
    { name: 'AES-GCM', iv }, await importar(key, ['encrypt']), datos,
  )
  return { ciphertext: bytesToBase64(new Uint8Array(cifrado)), iv: bytesToBase64(iv) }
}

export async function decryptBytes(key: Uint8Array, cifrado: Cifrado): Promise<Uint8Array> {
  const plano = await crypto.subtle.decrypt(
    { name: 'AES-GCM', iv: base64ToBytes(cifrado.iv) },
    await importar(key, ['decrypt']),
    base64ToBytes(cifrado.ciphertext),
  )
  return new Uint8Array(plano)
}
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `npm test -- aes`
Expected: PASS, 7 tests.

- [ ] **Step 5: Escribir el test de Vault Key que falla**

`resources/js/crypto/vault-key.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { generateVaultKey, wrapVaultKey, unwrapVaultKey } from './vault-key'
import { deriveMasterKey, deriveWrappingKey, generateSalt } from './kdf'

const hex = (b: Uint8Array) => Array.from(b).map(x => x.toString(16).padStart(2, '0')).join('')

async function wrappingKeyDe(password: string, salt: Uint8Array) {
  return deriveWrappingKey(await deriveMasterKey(password, {
    algo: 'pbkdf2-sha256', iterations: 1000, salt,
  }))
}

describe('Vault Key', () => {
  it('genera 32 bytes distintos en cada llamada', () => {
    const a = generateVaultKey(), b = generateVaultKey()
    expect(a.length).toBe(32)
    expect(hex(a)).not.toBe(hex(b))
  })

  it('desenvuelve exactamente la clave que envolvió', async () => {
    const salt = generateSalt()
    const wk = await wrappingKeyDe('master correcta', salt)
    const vk = generateVaultKey()
    expect(hex(await unwrapVaultKey(wk, await wrapVaultKey(wk, vk)))).toBe(hex(vk))
  })

  it('falla al desenvolver con una master password incorrecta', async () => {
    const salt = generateSalt()
    const envuelta = await wrapVaultKey(await wrappingKeyDe('correcta', salt), generateVaultKey())
    await expect(
      unwrapVaultKey(await wrappingKeyDe('incorrecta', salt), envuelta),
    ).rejects.toThrow()
  })

  it('permite rotar la master password sin cambiar la Vault Key', async () => {
    const saltViejo = generateSalt(), saltNuevo = generateSalt()
    const vk = generateVaultKey()
    const envueltaVieja = await wrapVaultKey(await wrappingKeyDe('vieja', saltViejo), vk)

    const recuperada = await unwrapVaultKey(await wrappingKeyDe('vieja', saltViejo), envueltaVieja)
    const envueltaNueva = await wrapVaultKey(await wrappingKeyDe('nueva', saltNuevo), recuperada)

    expect(hex(await unwrapVaultKey(await wrappingKeyDe('nueva', saltNuevo), envueltaNueva))).toBe(hex(vk))
  })
})
```

- [ ] **Step 6: Ejecutar y comprobar que falla**

Run: `npm test -- vault-key`
Expected: FAIL — no existe `./vault-key`.

- [ ] **Step 7: Implementar `resources/js/crypto/vault-key.ts`**

```ts
import { encryptBytes, decryptBytes, type Cifrado } from './aes'

const VAULT_KEY_BYTES = 32

/** Clave aleatoria que cifra todos los ítems. Se genera una sola vez, al crear la cuenta. */
export function generateVaultKey(): Uint8Array {
  return crypto.getRandomValues(new Uint8Array(VAULT_KEY_BYTES))
}

export function wrapVaultKey(wrappingKey: Uint8Array, vaultKey: Uint8Array): Promise<Cifrado> {
  return encryptBytes(wrappingKey, vaultKey)
}

export function unwrapVaultKey(wrappingKey: Uint8Array, envuelta: Cifrado): Promise<Uint8Array> {
  return decryptBytes(wrappingKey, envuelta)
}
```

- [ ] **Step 8: Ejecutar y comprobar que pasa**

Run: `npm test -- vault-key`
Expected: PASS, 4 tests.

- [ ] **Step 9: Commit**

```bash
git add resources/js/crypto/
git commit -m "Añade cifrado AES-256-GCM y envoltura de la Vault Key"
```

---

### Task 4: Cifrado de ítems

**Files:**
- Create: `resources/js/crypto/item-crypto.ts`
- Create: `resources/js/types/item.ts`
- Test: `resources/js/crypto/item-crypto.test.ts`

**Interfaces:**
- Consumes: `encryptBytes`, `decryptBytes`, `Cifrado` de `@/crypto/aes`.
- Produces:
  - `type ItemType = 'login' | 'api_key' | 'secure_text'`
  - `interface ItemPlano { type: ItemType; name: string; folder: string; favorite: boolean; fields: Record<string, string> }`
  - `interface ItemCifrado { id: string; ciphertext: string; iv: string; version: number; deleted_at: string | null; created_at: string; updated_at: string }`
  - `encryptItem(vaultKey: Uint8Array, item: ItemPlano): Promise<Cifrado>`
  - `decryptItem(vaultKey: Uint8Array, cifrado: Cifrado): Promise<ItemPlano>`

- [ ] **Step 1: Crear `resources/js/types/item.ts`**

```ts
export type ItemType = 'login' | 'api_key' | 'secure_text'

/** Contenido en claro de un ítem. Nunca abandona el navegador. */
export interface ItemPlano {
  type: ItemType
  name: string
  folder: string
  favorite: boolean
  fields: Record<string, string>
}

/** Lo que devuelve y acepta el servidor: opaco por completo. */
export interface ItemCifrado {
  id: string
  ciphertext: string
  iv: string
  version: number
  deleted_at: string | null
  created_at: string
  updated_at: string
}

/** Un ítem descifrado en memoria, con sus metadatos de servidor. */
export interface ItemDescifrado extends ItemPlano {
  id: string
  version: number
  deleted_at: string | null
  created_at: string
  updated_at: string
}

export const CAMPOS_POR_TIPO: Record<ItemType, string[]> = {
  login: ['url', 'username', 'password', 'notes'],
  api_key: ['service', 'key', 'secret', 'environment', 'expires_at', 'notes'],
  secure_text: ['content', 'notes'],
}

export const CAMPOS_SECRETOS: Record<ItemType, string[]> = {
  login: ['password'],
  api_key: ['key', 'secret'],
  secure_text: ['content'],
}
```

- [ ] **Step 2: Escribir el test que falla**

`resources/js/crypto/item-crypto.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { encryptItem, decryptItem } from './item-crypto'
import { generateVaultKey } from './vault-key'
import type { ItemPlano } from '@/types/item'

const login: ItemPlano = {
  type: 'login', name: 'GitHub', folder: 'dev', favorite: true,
  fields: { url: 'https://github.com', username: 'chris', password: 'p4ss w0rd ñ', notes: '' },
}

describe('cifrado de ítems', () => {
  it('descifra exactamente el ítem que cifró', async () => {
    const vk = generateVaultKey()
    expect(await decryptItem(vk, await encryptItem(vk, login))).toEqual(login)
  })

  it('conserva texto largo multilínea sin alterarlo', async () => {
    const vk = generateVaultKey()
    const contenido = '-----BEGIN PRIVATE KEY-----\nlínea1\nlínea2\n\n-----END PRIVATE KEY-----\n'
    const item: ItemPlano = {
      type: 'secure_text', name: 'SSH prod', folder: 'infra', favorite: false,
      fields: { content: contenido, notes: '' },
    }
    const resultado = await decryptItem(vk, await encryptItem(vk, item))
    expect(resultado.fields.content).toBe(contenido)
  })

  it('no deja el nombre ni el tipo legibles en el ciphertext', async () => {
    const vk = generateVaultKey()
    const { ciphertext } = await encryptItem(vk, login)
    expect(ciphertext).not.toContain('GitHub')
    expect(ciphertext).not.toContain('login')
    expect(atob(ciphertext)).not.toContain('GitHub')
  })

  it('falla al descifrar con otra Vault Key', async () => {
    const cifrado = await encryptItem(generateVaultKey(), login)
    await expect(decryptItem(generateVaultKey(), cifrado)).rejects.toThrow()
  })

  it('falla si el ciphertext ha sido manipulado', async () => {
    const vk = generateVaultKey()
    const cifrado = await encryptItem(vk, login)
    const roto = cifrado.ciphertext.slice(0, -8) + 'AAAAAAAA'
    await expect(decryptItem(vk, { ...cifrado, ciphertext: roto })).rejects.toThrow()
  })
})
```

- [ ] **Step 3: Ejecutar y comprobar que falla**

Run: `npm test -- item-crypto`
Expected: FAIL — no existe `./item-crypto`.

- [ ] **Step 4: Implementar `resources/js/crypto/item-crypto.ts`**

```ts
import { encryptBytes, decryptBytes, type Cifrado } from './aes'
import type { ItemPlano } from '@/types/item'

const enc = new TextEncoder()
const dec = new TextDecoder()

export function encryptItem(vaultKey: Uint8Array, item: ItemPlano): Promise<Cifrado> {
  return encryptBytes(vaultKey, enc.encode(JSON.stringify(item)))
}

export async function decryptItem(vaultKey: Uint8Array, cifrado: Cifrado): Promise<ItemPlano> {
  return JSON.parse(dec.decode(await decryptBytes(vaultKey, cifrado))) as ItemPlano
}
```

- [ ] **Step 5: Ejecutar y comprobar que pasa**

Run: `npm test -- item-crypto`
Expected: PASS, 5 tests.

- [ ] **Step 6: Ejecutar toda la batería**

Run: `npm test`
Expected: PASS, 30 tests en 5 ficheros.

- [ ] **Step 7: Commit**

```bash
git add resources/js/crypto/item-crypto.ts resources/js/crypto/item-crypto.test.ts resources/js/types/item.ts
git commit -m "Añade cifrado de ítems y tipos del modelo de datos"
```

---

### Task 5: Esquema de base de datos

**Files:**
- Modify: `database/migrations/0001_01_01_000000_create_users_table.php`
- Create: `database/migrations/2026_07_27_100000_create_webauthn_credentials_table.php`
- Create: `database/migrations/2026_07_27_100001_create_items_table.php`
- Modify: `app/Models/User.php`
- Create: `app/Models/WebauthnCredential.php`
- Create: `app/Models/Item.php`
- Test: `tests/Feature/EsquemaTest.php`

**Interfaces:**
- Consumes: nada.
- Produces: modelos `User`, `WebauthnCredential`, `Item`. `User` expone `salt`, `kdf_algo`, `kdf_iterations`, `auth_hash`, `wrapped_vault_key`, `vault_key_iv`, `auto_lock_seconds`, `setup_token`, `setup_token_expires_at`. `Item` expone `id` (uuid), `ciphertext`, `iv`, `version`, `deleted_at`.

- [ ] **Step 1: Escribir el test que falla**

`tests/Feature/EsquemaTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\Item;
use App\Models\User;
use App\Models\WebauthnCredential;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Schema;
use Tests\TestCase;

class EsquemaTest extends TestCase
{
    use RefreshDatabase;

    public function test_la_tabla_users_guarda_el_material_criptografico(): void
    {
        $usuario = User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'c2FsdA==',
            'kdf_algo' => 'pbkdf2-sha256',
            'kdf_iterations' => 600000,
            'auth_hash' => 'hash-argon2id',
            'wrapped_vault_key' => 'envuelta',
            'vault_key_iv' => 'iv',
        ]);

        $this->assertSame(600000, $usuario->fresh()->kdf_iterations);
        $this->assertSame(300, $usuario->fresh()->auto_lock_seconds);
    }

    public function test_la_tabla_items_no_tiene_ninguna_columna_de_contenido_en_claro(): void
    {
        $columnas = Schema::getColumnListing('items');

        $permitidas = ['id', 'user_id', 'ciphertext', 'iv', 'version',
                       'deleted_at', 'created_at', 'updated_at'];

        $this->assertEqualsCanonicalizing($permitidas, $columnas);
    }

    public function test_un_item_nace_con_version_uno(): void
    {
        $usuario = $this->usuarioDePrueba();
        $item = Item::create([
            'user_id' => $usuario->id,
            'ciphertext' => 'blob',
            'iv' => 'iv',
        ]);

        $this->assertSame(1, $item->fresh()->version);
        $this->assertIsString($item->id);
        $this->assertSame(36, strlen($item->id));
    }

    public function test_las_credenciales_webauthn_pertenecen_al_usuario(): void
    {
        $usuario = $this->usuarioDePrueba();
        WebauthnCredential::create([
            'user_id' => $usuario->id,
            'credential_id' => 'cred-1',
            'public_key' => 'clave-publica',
            'sign_count' => 0,
            'name' => 'Windows Hello',
        ]);

        $this->assertCount(1, $usuario->fresh()->webauthnCredentials);
    }

    private function usuarioDePrueba(): User
    {
        return User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'c2FsdA==',
            'kdf_algo' => 'pbkdf2-sha256',
            'kdf_iterations' => 600000,
            'auth_hash' => 'hash',
            'wrapped_vault_key' => 'envuelta',
            'vault_key_iv' => 'iv',
        ]);
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=EsquemaTest`
Expected: FAIL — las columnas no existen.

- [ ] **Step 3: Reescribir la migración de `users`**

Sustituye el contenido del método `up()` de `database/migrations/0001_01_01_000000_create_users_table.php`. La tabla `password_reset_tokens` se elimina por completo: en este sistema no existe recuperación de contraseña.

```php
public function up(): void
{
    Schema::create('users', function (Blueprint $table) {
        $table->id();
        $table->string('email')->unique();

        // Material criptográfico. Nada de esto permite descifrar por sí solo.
        $table->string('salt');                    // base64, 16 bytes
        $table->string('kdf_algo')->default('pbkdf2-sha256');
        $table->unsignedInteger('kdf_iterations')->default(600000);
        $table->string('auth_hash');               // Argon2id del auth hash recibido
        $table->text('wrapped_vault_key');
        $table->string('vault_key_iv');

        $table->unsignedInteger('auto_lock_seconds')->default(300);

        // Bootstrap de un solo uso.
        $table->string('setup_token')->nullable();
        $table->timestamp('setup_token_expires_at')->nullable();

        $table->rememberToken();
        $table->timestamps();
    });

    Schema::create('sessions', function (Blueprint $table) {
        $table->string('id')->primary();
        $table->foreignId('user_id')->nullable()->index();
        $table->string('ip_address', 45)->nullable();
        $table->text('user_agent')->nullable();
        $table->longText('payload');
        $table->integer('last_activity')->index();
    });
}

public function down(): void
{
    Schema::dropIfExists('users');
    Schema::dropIfExists('sessions');
}
```

- [ ] **Step 4: Crear la migración de `webauthn_credentials`**

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('webauthn_credentials', function (Blueprint $table) {
            $table->id();
            $table->foreignId('user_id')->constrained()->cascadeOnDelete();
            $table->string('credential_id')->unique();
            $table->text('public_key');
            $table->unsignedBigInteger('sign_count')->default(0);
            $table->string('name');
            $table->timestamp('last_used_at')->nullable();
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('webauthn_credentials');
    }
};
```

- [ ] **Step 5: Crear la migración de `items`**

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('items', function (Blueprint $table) {
            $table->uuid('id')->primary();
            $table->foreignId('user_id')->constrained()->cascadeOnDelete();

            // Todo el contenido vive aquí dentro, cifrado. El servidor no lo entiende.
            $table->longText('ciphertext');
            $table->string('iv');

            $table->unsignedInteger('version')->default(1);
            $table->softDeletes();
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('items');
    }
};
```

- [ ] **Step 6: Reescribir `app/Models/User.php`**

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Relations\HasMany;
use Illuminate\Foundation\Auth\User as Authenticatable;
use Illuminate\Notifications\Notifiable;

class User extends Authenticatable
{
    use Notifiable;

    protected $fillable = [
        'email', 'salt', 'kdf_algo', 'kdf_iterations', 'auth_hash',
        'wrapped_vault_key', 'vault_key_iv', 'auto_lock_seconds',
        'setup_token', 'setup_token_expires_at',
    ];

    protected $hidden = ['auth_hash', 'remember_token', 'setup_token'];

    protected function casts(): array
    {
        return [
            'kdf_iterations' => 'integer',
            'auto_lock_seconds' => 'integer',
            'setup_token_expires_at' => 'datetime',
        ];
    }

    public function webauthnCredentials(): HasMany
    {
        return $this->hasMany(WebauthnCredential::class);
    }

    public function items(): HasMany
    {
        return $this->hasMany(Item::class);
    }
}
```

- [ ] **Step 7: Crear `app/Models/WebauthnCredential.php`**

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class WebauthnCredential extends Model
{
    protected $fillable = ['user_id', 'credential_id', 'public_key', 'sign_count', 'name', 'last_used_at'];

    protected function casts(): array
    {
        return ['sign_count' => 'integer', 'last_used_at' => 'datetime'];
    }

    public function user(): BelongsTo
    {
        return $this->belongsTo(User::class);
    }
}
```

- [ ] **Step 8: Crear `app/Models/Item.php`**

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Concerns\HasUuids;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\SoftDeletes;

class Item extends Model
{
    use HasUuids, SoftDeletes;

    protected $fillable = ['id', 'user_id', 'ciphertext', 'iv', 'version'];

    protected function casts(): array
    {
        return ['version' => 'integer'];
    }

    public function user(): BelongsTo
    {
        return $this->belongsTo(User::class);
    }
}
```

- [ ] **Step 9: Borrar la migración de reseteo de contraseña si quedó separada**

Comprueba que no exista ninguna tabla `password_reset_tokens` en las migraciones. Si la hay, elimínala: no tiene sentido en este sistema.

Run: `grep -rn "password_reset_tokens" database/migrations/ || echo "limpio"`

- [ ] **Step 10: Ejecutar y comprobar que pasa**

Run: `php artisan migrate:fresh && php artisan test --filter=EsquemaTest`
Expected: PASS, 4 tests.

- [ ] **Step 11: Commit**

```bash
git add database/migrations app/Models tests/Feature/EsquemaTest.php
git commit -m "Define el esquema: usuario con material criptográfico, passkeys e ítems opacos"
```

---

### Task 6: Comando de bootstrap `vault:init`

**Files:**
- Create: `app/Console/Commands/VaultInit.php`
- Test: `tests/Feature/VaultInitTest.php`

**Interfaces:**
- Consumes: modelo `User`.
- Produces: comando `vault:init --email=`, que crea el usuario si no existe y emite un `setup_token` válido 15 minutos.

- [ ] **Step 1: Escribir el test que falla**

`tests/Feature/VaultInitTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class VaultInitTest extends TestCase
{
    use RefreshDatabase;

    public function test_crea_el_usuario_y_emite_un_token_de_setup(): void
    {
        $this->artisan('vault:init', ['--email' => 'yo@ejemplo.com'])->assertSuccessful();

        $usuario = User::firstWhere('email', 'yo@ejemplo.com');
        $this->assertNotNull($usuario);
        $this->assertNotNull($usuario->setup_token);
        $this->assertTrue($usuario->setup_token_expires_at->isFuture());
    }

    public function test_rechaza_crear_un_segundo_usuario(): void
    {
        $this->artisan('vault:init', ['--email' => 'primero@ejemplo.com'])->assertSuccessful();
        $this->artisan('vault:init', ['--email' => 'segundo@ejemplo.com'])->assertFailed();

        $this->assertSame(1, User::count());
    }

    public function test_reemite_el_token_si_la_cuenta_sigue_sin_completar(): void
    {
        $this->artisan('vault:init', ['--email' => 'yo@ejemplo.com'])->assertSuccessful();
        $primero = User::first()->setup_token;

        $this->artisan('vault:init', ['--email' => 'yo@ejemplo.com'])->assertSuccessful();

        $this->assertNotSame($primero, User::first()->setup_token);
    }

    public function test_rechaza_reemitir_si_la_cuenta_ya_esta_completa(): void
    {
        $this->artisan('vault:init', ['--email' => 'yo@ejemplo.com'])->assertSuccessful();
        User::first()->update([
            'auth_hash' => 'ya-configurada',
            'setup_token' => null,
        ]);

        $this->artisan('vault:init', ['--email' => 'yo@ejemplo.com'])->assertFailed();
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=VaultInitTest`
Expected: FAIL — el comando no existe.

- [ ] **Step 3: Implementar el comando**

`app/Console/Commands/VaultInit.php`:

```php
<?php

namespace App\Console\Commands;

use App\Models\User;
use Illuminate\Console\Command;
use Illuminate\Support\Str;

class VaultInit extends Command
{
    protected $signature = 'vault:init {--email= : Correo con el que iniciarás sesión}';

    protected $description = 'Crea la única cuenta del sistema y emite un enlace de configuración de un solo uso';

    public function handle(): int
    {
        $email = $this->option('email') ?: $this->ask('Correo');

        if (! filter_var($email, FILTER_VALIDATE_EMAIL)) {
            $this->error('El correo no es válido.');

            return self::FAILURE;
        }

        $existente = User::first();

        if ($existente && $existente->email !== $email) {
            $this->error("Ya existe una cuenta ({$existente->email}). Este sistema es de usuario único.");

            return self::FAILURE;
        }

        if ($existente && $existente->auth_hash !== '') {
            $this->error('La cuenta ya está configurada. Para reconfigurarla habría que destruir la bóveda.');

            return self::FAILURE;
        }

        $token = Str::random(64);

        $usuario = $existente ?? new User(['email' => $email]);
        $usuario->fill([
            'email' => $email,
            'salt' => '',
            'auth_hash' => '',
            'wrapped_vault_key' => '',
            'vault_key_iv' => '',
            'setup_token' => hash('sha256', $token),
            'setup_token_expires_at' => now()->addMinutes(15),
        ])->save();

        $url = rtrim(config('app.url'), '/')."/setup?token={$token}";

        $this->newLine();
        $this->info('Cuenta preparada. Abre este enlace en el navegador durante los próximos 15 minutos:');
        $this->newLine();
        $this->line($url);
        $this->newLine();
        $this->warn('La master password que elijas no se puede recuperar. Guárdala fuera de este sistema.');

        return self::SUCCESS;
    }
}
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `php artisan test --filter=VaultInitTest`
Expected: PASS, 4 tests.

- [ ] **Step 5: Commit**

```bash
git add app/Console/Commands/VaultInit.php tests/Feature/VaultInitTest.php
git commit -m "Añade el comando vault:init con enlace de configuración de un solo uso"
```

---

### Task 7: Endpoint de configuración inicial

**Files:**
- Create: `app/Http/Controllers/SetupController.php`
- Create: `app/Http/Requests/SetupRequest.php`
- Modify: `routes/web.php`
- Create: `routes/api.php` (si no existe, registrarlo en `bootstrap/app.php`)
- Test: `tests/Feature/SetupTest.php`

**Interfaces:**
- Consumes: modelo `User`.
- Produces:
  - `POST /api/setup` con `{token, salt, kdf_algo, kdf_iterations, auth_hash, wrapped_vault_key, vault_key_iv}` → 204.
  - Tras completarse, `setup_token` queda a `null` y el endpoint responde 403 para siempre.

- [ ] **Step 1: Habilitar rutas de API**

Run: `php artisan install:api --without-migration-prompt` — si el comando no existe en Laravel 13, crea `routes/api.php` manualmente y regístralo en `bootstrap/app.php` dentro de `withRouting(api: __DIR__.'/../routes/api.php')`.

Verifica con: `php artisan route:list --path=api`

- [ ] **Step 2: Escribir el test que falla**

`tests/Feature/SetupTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Str;
use Tests\TestCase;

class SetupTest extends TestCase
{
    use RefreshDatabase;

    private string $token;

    protected function setUp(): void
    {
        parent::setUp();
        $this->token = Str::random(64);

        User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => '', 'auth_hash' => '', 'wrapped_vault_key' => '', 'vault_key_iv' => '',
            'setup_token' => hash('sha256', $this->token),
            'setup_token_expires_at' => now()->addMinutes(15),
        ]);
    }

    private function payload(array $sobrescribir = []): array
    {
        return array_merge([
            'token' => $this->token,
            'salt' => base64_encode(random_bytes(16)),
            'kdf_algo' => 'pbkdf2-sha256',
            'kdf_iterations' => 600000,
            'auth_hash' => base64_encode(random_bytes(32)),
            'wrapped_vault_key' => base64_encode(random_bytes(48)),
            'vault_key_iv' => base64_encode(random_bytes(12)),
        ], $sobrescribir);
    }

    public function test_completa_la_configuracion_y_quema_el_token(): void
    {
        $this->postJson('/api/setup', $this->payload())->assertNoContent();

        $usuario = User::first();
        $this->assertNotSame('', $usuario->auth_hash);
        $this->assertNull($usuario->setup_token);
    }

    public function test_guarda_el_auth_hash_hasheado_y_no_en_claro(): void
    {
        $payload = $this->payload();
        $this->postJson('/api/setup', $payload)->assertNoContent();

        $this->assertNotSame($payload['auth_hash'], User::first()->auth_hash);
        $this->assertStringStartsWith('$argon2id$', User::first()->auth_hash);
    }

    public function test_rechaza_un_token_incorrecto(): void
    {
        $this->postJson('/api/setup', $this->payload(['token' => 'no-es-el-token']))
            ->assertForbidden();
    }

    public function test_rechaza_un_token_caducado(): void
    {
        User::first()->update(['setup_token_expires_at' => now()->subMinute()]);

        $this->postJson('/api/setup', $this->payload())->assertForbidden();
    }

    public function test_no_permite_configurar_dos_veces(): void
    {
        $this->postJson('/api/setup', $this->payload())->assertNoContent();
        $this->postJson('/api/setup', $this->payload())->assertForbidden();
    }

    public function test_exige_un_minimo_de_iteraciones(): void
    {
        $this->postJson('/api/setup', $this->payload(['kdf_iterations' => 1000]))
            ->assertStatus(422);
    }

    public function test_rechaza_campos_que_no_sean_base64(): void
    {
        $this->postJson('/api/setup', $this->payload(['salt' => 'no es base64 !!']))
            ->assertStatus(422);
    }
}
```

- [ ] **Step 3: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=SetupTest`
Expected: FAIL — la ruta no existe (404).

- [ ] **Step 4: Crear `app/Http/Requests/SetupRequest.php`**

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Validation\Rule;

class SetupRequest extends FormRequest
{
    public function authorize(): bool
    {
        return true;
    }

    public function rules(): array
    {
        return [
            'token' => ['required', 'string'],
            'salt' => ['required', 'string', 'base64'],
            'kdf_algo' => ['required', Rule::in(['pbkdf2-sha256'])],
            'kdf_iterations' => ['required', 'integer', 'min:600000'],
            'auth_hash' => ['required', 'string', 'base64'],
            'wrapped_vault_key' => ['required', 'string', 'base64'],
            'vault_key_iv' => ['required', 'string', 'base64'],
        ];
    }
}
```

Si la regla `base64` no existe en esta versión de Laravel, sustitúyela por una closure que verifique `base64_decode($valor, true) !== false`.

- [ ] **Step 5: Crear `app/Http/Controllers/SetupController.php`**

```php
<?php

namespace App\Http\Controllers;

use App\Http\Requests\SetupRequest;
use App\Models\User;
use Illuminate\Http\Response;
use Illuminate\Support\Facades\Hash;

class SetupController extends Controller
{
    public function store(SetupRequest $peticion): Response
    {
        $usuario = User::first();

        abort_if($usuario === null, 403);
        abort_if($usuario->setup_token === null, 403, 'La cuenta ya está configurada.');
        abort_if($usuario->setup_token_expires_at?->isPast() ?? true, 403, 'El enlace ha caducado.');

        abort_unless(
            hash_equals($usuario->setup_token, hash('sha256', $peticion->string('token')->toString())),
            403,
        );

        $usuario->update([
            'salt' => $peticion->string('salt')->toString(),
            'kdf_algo' => $peticion->string('kdf_algo')->toString(),
            'kdf_iterations' => $peticion->integer('kdf_iterations'),
            // El auth hash llega derivado del cliente; aquí se vuelve a hashear
            // para que un volcado de la base de datos no entregue una credencial usable.
            'auth_hash' => Hash::make($peticion->string('auth_hash')->toString()),
            'wrapped_vault_key' => $peticion->string('wrapped_vault_key')->toString(),
            'vault_key_iv' => $peticion->string('vault_key_iv')->toString(),
            'setup_token' => null,
            'setup_token_expires_at' => null,
        ]);

        return response()->noContent();
    }
}
```

- [ ] **Step 6: Configurar Argon2id como driver de hash**

En `config/hashing.php` pon `'driver' => 'argon2id'` y ajusta `argon`: `'memory' => 65536, 'threads' => 4, 'time' => 4`.

- [ ] **Step 7: Registrar la ruta**

En `routes/api.php`:

```php
use App\Http\Controllers\SetupController;
use Illuminate\Support\Facades\Route;

Route::post('/setup', [SetupController::class, 'store'])->middleware('throttle:10,1');
```

- [ ] **Step 8: Ejecutar y comprobar que pasa**

Run: `php artisan test --filter=SetupTest`
Expected: PASS, 7 tests.

- [ ] **Step 9: Commit**

```bash
git add app/Http routes config/hashing.php tests/Feature/SetupTest.php
git commit -m "Añade el endpoint de configuración inicial con token de un solo uso"
```

---

### Task 8: Prelogin y login

**Files:**
- Create: `app/Http/Controllers/AuthController.php`
- Modify: `routes/api.php`
- Test: `tests/Feature/LoginTest.php`

**Interfaces:**
- Consumes: modelo `User`.
- Produces:
  - `POST /api/auth/prelogin` `{email}` → `{salt, kdf_algo, kdf_iterations}`
  - `POST /api/auth/login` `{email, auth_hash}` → `{webauthn_required: true}` y marca la sesión como semiautenticada. **No** devuelve `wrapped_vault_key` todavía: eso llega solo tras la passkey (Task 9).

- [ ] **Step 1: Escribir el test que falla**

`tests/Feature/LoginTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Hash;
use Illuminate\Support\Facades\RateLimiter;
use Tests\TestCase;

class LoginTest extends TestCase
{
    use RefreshDatabase;

    private string $authHash = 'YXV0aC1oYXNoLWRlLXBydWViYQ==';

    protected function setUp(): void
    {
        parent::setUp();
        RateLimiter::clear('login:yo@ejemplo.com');

        User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'c2FsdC1kZS1wcnVlYmE=',
            'kdf_algo' => 'pbkdf2-sha256',
            'kdf_iterations' => 600000,
            'auth_hash' => Hash::make($this->authHash),
            'wrapped_vault_key' => 'ZW52dWVsdGE=',
            'vault_key_iv' => 'aXY=',
        ]);
    }

    public function test_prelogin_devuelve_los_parametros_kdf(): void
    {
        $this->postJson('/api/auth/prelogin', ['email' => 'yo@ejemplo.com'])
            ->assertOk()
            ->assertJson([
                'salt' => 'c2FsdC1kZS1wcnVlYmE=',
                'kdf_algo' => 'pbkdf2-sha256',
                'kdf_iterations' => 600000,
            ]);
    }

    public function test_prelogin_nunca_expone_el_auth_hash_ni_la_vault_key(): void
    {
        $respuesta = $this->postJson('/api/auth/prelogin', ['email' => 'yo@ejemplo.com']);

        $respuesta->assertJsonMissingPath('auth_hash');
        $respuesta->assertJsonMissingPath('wrapped_vault_key');
    }

    public function test_login_acepta_el_auth_hash_correcto(): void
    {
        $this->postJson('/api/auth/login', [
            'email' => 'yo@ejemplo.com',
            'auth_hash' => $this->authHash,
        ])->assertOk()->assertJson(['webauthn_required' => true]);
    }

    public function test_login_no_entrega_la_vault_key_antes_de_la_passkey(): void
    {
        $this->postJson('/api/auth/login', [
            'email' => 'yo@ejemplo.com',
            'auth_hash' => $this->authHash,
        ])->assertJsonMissingPath('wrapped_vault_key');
    }

    public function test_login_rechaza_un_auth_hash_incorrecto(): void
    {
        $this->postJson('/api/auth/login', [
            'email' => 'yo@ejemplo.com',
            'auth_hash' => 'aW5jb3JyZWN0bw==',
        ])->assertStatus(422);
    }

    public function test_login_bloquea_tras_intentos_repetidos(): void
    {
        for ($i = 0; $i < 5; $i++) {
            $this->postJson('/api/auth/login', [
                'email' => 'yo@ejemplo.com',
                'auth_hash' => 'aW5jb3JyZWN0bw==',
            ]);
        }

        $this->postJson('/api/auth/login', [
            'email' => 'yo@ejemplo.com',
            'auth_hash' => $this->authHash,
        ])->assertStatus(429);
    }

    public function test_un_email_desconocido_no_revela_que_no_existe(): void
    {
        $this->postJson('/api/auth/prelogin', ['email' => 'otro@ejemplo.com'])
            ->assertOk()
            ->assertJsonStructure(['salt', 'kdf_algo', 'kdf_iterations']);
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=LoginTest`
Expected: FAIL — la ruta no existe.

- [ ] **Step 3: Implementar `AuthController`**

El prelogin de un correo desconocido devuelve un salt falso pero **estable** (derivado con HMAC de `APP_KEY`), para que la respuesta no delate si la cuenta existe ni cambie entre peticiones.

```php
<?php

namespace App\Http\Controllers;

use App\Models\User;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Hash;
use Illuminate\Support\Facades\RateLimiter;
use Illuminate\Validation\ValidationException;

class AuthController extends Controller
{
    public function prelogin(Request $peticion): JsonResponse
    {
        $datos = $peticion->validate(['email' => ['required', 'email']]);

        $usuario = User::firstWhere('email', $datos['email']);

        if ($usuario === null) {
            return response()->json([
                'salt' => base64_encode(substr(hash_hmac('sha256', $datos['email'], config('app.key'), true), 0, 16)),
                'kdf_algo' => 'pbkdf2-sha256',
                'kdf_iterations' => 600000,
            ]);
        }

        return response()->json([
            'salt' => $usuario->salt,
            'kdf_algo' => $usuario->kdf_algo,
            'kdf_iterations' => $usuario->kdf_iterations,
        ]);
    }

    public function login(Request $peticion): JsonResponse
    {
        $datos = $peticion->validate([
            'email' => ['required', 'email'],
            'auth_hash' => ['required', 'string'],
        ]);

        $clave = 'login:'.$datos['email'];

        if (RateLimiter::tooManyAttempts($clave, 5)) {
            abort(429, 'Demasiados intentos. Espera '.RateLimiter::availableIn($clave).' segundos.');
        }

        $usuario = User::firstWhere('email', $datos['email']);

        if ($usuario === null || ! Hash::check($datos['auth_hash'], $usuario->auth_hash)) {
            RateLimiter::hit($clave, 300);

            throw ValidationException::withMessages([
                'auth_hash' => 'Credenciales incorrectas.',
            ]);
        }

        RateLimiter::clear($clave);

        // Semiautenticado: falta la passkey antes de entregar la Vault Key envuelta.
        $peticion->session()->put('semiautenticado', $usuario->id);

        return response()->json(['webauthn_required' => true]);
    }

    public function logout(Request $peticion): JsonResponse
    {
        $peticion->session()->flush();
        $peticion->session()->regenerate();

        return response()->json(['ok' => true]);
    }
}
```

- [ ] **Step 4: Registrar las rutas**

En `routes/api.php`:

```php
use App\Http\Controllers\AuthController;

Route::post('/auth/prelogin', [AuthController::class, 'prelogin'])->middleware('throttle:20,1');
Route::post('/auth/login', [AuthController::class, 'login'])->middleware('throttle:20,1');
Route::post('/auth/logout', [AuthController::class, 'logout']);
```

Las rutas de API necesitan sesión con estado. En `bootstrap/app.php`, dentro de `withMiddleware`, añade:

```php
$middleware->api(prepend: [
    \Illuminate\Cookie\Middleware\AddQueuedCookiesToResponse::class,
    \Illuminate\Session\Middleware\StartSession::class,
]);
```

- [ ] **Step 5: Ejecutar y comprobar que pasa**

Run: `php artisan test --filter=LoginTest`
Expected: PASS, 7 tests.

- [ ] **Step 6: Commit**

```bash
git add app/Http/Controllers/AuthController.php routes/api.php bootstrap/app.php tests/Feature/LoginTest.php
git commit -m "Añade prelogin y login con verificación del auth hash y throttling"
```

---

### Task 9: WebAuthn

**Files:**
- Create: `app/Services/WebauthnService.php`
- Create: `app/Http/Controllers/WebauthnController.php`
- Modify: `routes/api.php`
- Test: `tests/Feature/WebauthnTest.php`

**Interfaces:**
- Consumes: sesión `semiautenticado` de Task 8, modelo `WebauthnCredential`.
- Produces:
  - `POST /api/auth/webauthn/challenge` → opciones de aserción
  - `POST /api/auth/webauthn/verify` → `{wrapped_vault_key, vault_key_iv, auto_lock_seconds}` y sesión autenticada
  - `POST /api/account/passkeys/challenge` y `POST /api/account/passkeys` para registrar
  - `GET /api/account/passkeys`, `DELETE /api/account/passkeys/{id}`

- [ ] **Step 1: Instalar y verificar compatibilidad de la librería**

```bash
composer require web-auth/webauthn-lib
```

Si Composer rechaza la instalación por incompatibilidad con Laravel 13 o PHP 8.4, **detente y reporta el conflicto** en lugar de forzar `--ignore-platform-reqs`. Anota la versión instalada:

Run: `composer show web-auth/webauthn-lib | head -3`

- [ ] **Step 2: Escribir el test que falla**

Los tests no simulan el navegador: verifican el control de acceso y las invariantes de sesión, que es donde están los fallos reales de seguridad.

`tests/Feature/WebauthnTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\User;
use App\Models\WebauthnCredential;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Hash;
use Tests\TestCase;

class WebauthnTest extends TestCase
{
    use RefreshDatabase;

    private User $usuario;

    protected function setUp(): void
    {
        parent::setUp();

        $this->usuario = User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'c2FsdA==', 'kdf_algo' => 'pbkdf2-sha256', 'kdf_iterations' => 600000,
            'auth_hash' => Hash::make('YXV0aA=='),
            'wrapped_vault_key' => 'ZW52dWVsdGE=', 'vault_key_iv' => 'aXY=',
        ]);

        WebauthnCredential::create([
            'user_id' => $this->usuario->id,
            'credential_id' => 'cred-1', 'public_key' => 'pk', 'sign_count' => 0,
            'name' => 'Windows Hello',
        ]);
    }

    public function test_el_reto_exige_haber_pasado_el_login(): void
    {
        $this->postJson('/api/auth/webauthn/challenge')->assertStatus(403);
    }

    public function test_el_reto_se_emite_tras_el_login(): void
    {
        $this->withSession(['semiautenticado' => $this->usuario->id])
            ->postJson('/api/auth/webauthn/challenge')
            ->assertOk()
            ->assertJsonStructure(['challenge', 'allowCredentials']);
    }

    public function test_la_verificacion_falla_sin_reto_previo(): void
    {
        $this->withSession(['semiautenticado' => $this->usuario->id])
            ->postJson('/api/auth/webauthn/verify', ['assertion' => []])
            ->assertStatus(403);
    }

    public function test_la_vault_key_solo_se_entrega_a_una_sesion_autenticada(): void
    {
        $this->getJson('/api/vault/items')->assertStatus(401);
    }

    public function test_listar_passkeys_exige_sesion_autenticada(): void
    {
        $this->getJson('/api/account/passkeys')->assertStatus(401);
    }

    public function test_no_se_puede_borrar_la_ultima_passkey(): void
    {
        $this->actingAs($this->usuario)
            ->deleteJson('/api/account/passkeys/'.WebauthnCredential::first()->id)
            ->assertStatus(422);
    }

    public function test_se_puede_borrar_una_passkey_si_queda_otra(): void
    {
        WebauthnCredential::create([
            'user_id' => $this->usuario->id,
            'credential_id' => 'cred-2', 'public_key' => 'pk2', 'sign_count' => 0,
            'name' => 'iPhone',
        ]);

        $this->actingAs($this->usuario)
            ->deleteJson('/api/account/passkeys/'.WebauthnCredential::first()->id)
            ->assertNoContent();

        $this->assertSame(1, WebauthnCredential::count());
    }
}
```

- [ ] **Step 3: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=WebauthnTest`
Expected: FAIL — las rutas no existen.

- [ ] **Step 4: Implementar `WebauthnService`**

Consulta la documentación de la versión instalada antes de escribir esto — la API de `web-auth/webauthn-lib` cambia entre majors. El servicio debe exponer exactamente estos cuatro métodos:

```php
<?php

namespace App\Services;

use App\Models\User;

class WebauthnService
{
    /** Opciones para registrar una passkey nueva. Guarda el reto en sesión. */
    public function opcionesDeRegistro(User $usuario): array { /* ... */ }

    /** Valida la respuesta del navegador y persiste la credencial. */
    public function registrar(User $usuario, array $credencial, string $nombre): void { /* ... */ }

    /** Opciones de aserción para iniciar sesión. Guarda el reto en sesión. */
    public function opcionesDeAsercion(User $usuario): array { /* ... */ }

    /** Valida la aserción; devuelve true y actualiza sign_count si es correcta. */
    public function verificar(User $usuario, array $asercion): bool { /* ... */ }
}
```

El `rpId` sale del host de `config('app.url')`. El reto se guarda en `session('webauthn_challenge')` y se borra tras cada uso, sea válido o no.

- [ ] **Step 5: Implementar `WebauthnController`**

Puntos que los tests exigen y que no puedes omitir:
- `challenge` y `verify` abortan con 403 si no hay `semiautenticado` en sesión.
- `verify` aborta con 403 si no hay reto guardado en sesión.
- Tras verificar: `Auth::login($usuario)`, `session()->regenerate()`, se borra `semiautenticado`, y se devuelve `{wrapped_vault_key, vault_key_iv, auto_lock_seconds}`.
- `destroy` de una passkey aborta con 422 si es la última que queda.

- [ ] **Step 6: Registrar las rutas**

```php
Route::post('/auth/webauthn/challenge', [WebauthnController::class, 'challenge']);
Route::post('/auth/webauthn/verify', [WebauthnController::class, 'verify'])->middleware('throttle:20,1');

Route::middleware('auth')->group(function () {
    Route::get('/account/passkeys', [WebauthnController::class, 'index']);
    Route::post('/account/passkeys/challenge', [WebauthnController::class, 'registroChallenge']);
    Route::post('/account/passkeys', [WebauthnController::class, 'store']);
    Route::delete('/account/passkeys/{credencial}', [WebauthnController::class, 'destroy']);
});
```

- [ ] **Step 7: Ejecutar y comprobar que pasa**

Run: `php artisan test --filter=WebauthnTest`
Expected: PASS, 7 tests.

- [ ] **Step 8: Commit**

```bash
git add app/Services app/Http/Controllers/WebauthnController.php routes/api.php tests/Feature/WebauthnTest.php
git commit -m "Añade autenticación WebAuthn como segundo factor obligatorio"
```

---

### Task 10: CRUD de ítems con control de concurrencia

**Files:**
- Create: `app/Http/Controllers/ItemController.php`
- Create: `app/Http/Requests/ItemRequest.php`
- Modify: `routes/api.php`
- Test: `tests/Feature/ItemTest.php`

**Interfaces:**
- Consumes: modelo `Item`, middleware `auth`.
- Produces:
  - `GET /api/vault/items` → `{items: ItemCifrado[]}` incluida la papelera
  - `POST /api/vault/items` `{id, ciphertext, iv}` → 201 con el ítem
  - `PUT /api/vault/items/{id}` `{ciphertext, iv, version}` → 200, o **409** si `version` no coincide
  - `DELETE /api/vault/items/{id}` → 204 (soft delete)
  - `POST /api/vault/items/{id}/restore` → 200
  - `GET /api/vault/export` → `{items: ItemCifrado[]}`

- [ ] **Step 1: Escribir el test que falla**

`tests/Feature/ItemTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\Item;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Str;
use Tests\TestCase;

class ItemTest extends TestCase
{
    use RefreshDatabase;

    private User $usuario;

    protected function setUp(): void
    {
        parent::setUp();
        $this->usuario = User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'c2FsdA==', 'kdf_algo' => 'pbkdf2-sha256', 'kdf_iterations' => 600000,
            'auth_hash' => 'hash', 'wrapped_vault_key' => 'ZW52', 'vault_key_iv' => 'aXY=',
        ]);
    }

    private function crear(): Item
    {
        return Item::create([
            'id' => (string) Str::uuid(),
            'user_id' => $this->usuario->id,
            'ciphertext' => base64_encode(random_bytes(64)),
            'iv' => base64_encode(random_bytes(12)),
        ]);
    }

    public function test_exige_autenticacion(): void
    {
        $this->getJson('/api/vault/items')->assertStatus(401);
        $this->postJson('/api/vault/items', [])->assertStatus(401);
    }

    public function test_crea_un_item_con_el_uuid_que_envia_el_cliente(): void
    {
        $id = (string) Str::uuid();

        $this->actingAs($this->usuario)->postJson('/api/vault/items', [
            'id' => $id,
            'ciphertext' => base64_encode(random_bytes(64)),
            'iv' => base64_encode(random_bytes(12)),
        ])->assertCreated()->assertJsonPath('id', $id);

        $this->assertSame(1, Item::count());
    }

    public function test_lista_los_items_del_usuario_incluida_la_papelera(): void
    {
        $this->crear();
        $this->crear()->delete();

        $this->actingAs($this->usuario)->getJson('/api/vault/items')
            ->assertOk()
            ->assertJsonCount(2, 'items');
    }

    public function test_actualizar_incrementa_la_version(): void
    {
        $item = $this->crear();

        $this->actingAs($this->usuario)->putJson("/api/vault/items/{$item->id}", [
            'ciphertext' => base64_encode(random_bytes(64)),
            'iv' => base64_encode(random_bytes(12)),
            'version' => 1,
        ])->assertOk()->assertJsonPath('version', 2);
    }

    public function test_actualizar_con_una_version_antigua_devuelve_409_y_no_sobrescribe(): void
    {
        $item = $this->crear();
        $original = $item->ciphertext;

        $this->actingAs($this->usuario)->putJson("/api/vault/items/{$item->id}", [
            'ciphertext' => base64_encode(random_bytes(64)),
            'iv' => base64_encode(random_bytes(12)),
            'version' => 99,
        ])->assertStatus(409);

        $this->assertSame($original, $item->fresh()->ciphertext);
    }

    public function test_borrar_manda_a_la_papelera_sin_destruir(): void
    {
        $item = $this->crear();

        $this->actingAs($this->usuario)->deleteJson("/api/vault/items/{$item->id}")
            ->assertNoContent();

        $this->assertNotNull($item->fresh()->deleted_at);
    }

    public function test_restaurar_saca_de_la_papelera(): void
    {
        $item = $this->crear();
        $item->delete();

        $this->actingAs($this->usuario)->postJson("/api/vault/items/{$item->id}/restore")
            ->assertOk();

        $this->assertNull($item->fresh()->deleted_at);
    }

    public function test_rechaza_ciphertext_que_no_sea_base64(): void
    {
        $this->actingAs($this->usuario)->postJson('/api/vault/items', [
            'id' => (string) Str::uuid(),
            'ciphertext' => 'esto no es base64 !!!',
            'iv' => base64_encode(random_bytes(12)),
        ])->assertStatus(422);
    }

    public function test_el_export_devuelve_solo_blobs_cifrados(): void
    {
        $this->crear();

        $respuesta = $this->actingAs($this->usuario)->getJson('/api/vault/export')->assertOk();

        foreach ($respuesta->json('items') as $item) {
            $this->assertArrayHasKey('ciphertext', $item);
            $this->assertArrayNotHasKey('name', $item);
            $this->assertArrayNotHasKey('fields', $item);
        }
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=ItemTest`
Expected: FAIL — las rutas no existen.

- [ ] **Step 3: Crear `app/Http/Requests/ItemRequest.php`**

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class ItemRequest extends FormRequest
{
    public function authorize(): bool
    {
        return $this->user() !== null;
    }

    public function rules(): array
    {
        $reglas = [
            'ciphertext' => ['required', 'string', 'base64', 'max:1048576'],
            'iv' => ['required', 'string', 'base64'],
        ];

        if ($this->isMethod('POST')) {
            $reglas['id'] = ['required', 'uuid'];
        }

        if ($this->isMethod('PUT')) {
            $reglas['version'] = ['required', 'integer', 'min:1'];
        }

        return $reglas;
    }
}
```

- [ ] **Step 4: Implementar `ItemController`**

```php
<?php

namespace App\Http\Controllers;

use App\Http\Requests\ItemRequest;
use App\Models\Item;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Http\Response;

class ItemController extends Controller
{
    public function index(Request $peticion): JsonResponse
    {
        return response()->json([
            'items' => $peticion->user()->items()->withTrashed()->get(),
        ]);
    }

    public function store(ItemRequest $peticion): JsonResponse
    {
        $item = $peticion->user()->items()->create([
            'id' => $peticion->string('id')->toString(),
            'ciphertext' => $peticion->string('ciphertext')->toString(),
            'iv' => $peticion->string('iv')->toString(),
            'version' => 1,
        ]);

        return response()->json($item, 201);
    }

    public function update(ItemRequest $peticion, string $id): JsonResponse
    {
        $item = $peticion->user()->items()->withTrashed()->findOrFail($id);

        // Concurrencia optimista: si otro dispositivo escribió antes, no pisamos nada.
        $afectadas = $peticion->user()->items()
            ->whereKey($id)
            ->where('version', $peticion->integer('version'))
            ->update([
                'ciphertext' => $peticion->string('ciphertext')->toString(),
                'iv' => $peticion->string('iv')->toString(),
                'version' => $peticion->integer('version') + 1,
                'updated_at' => now(),
            ]);

        if ($afectadas === 0) {
            return response()->json([
                'message' => 'El ítem cambió desde otro dispositivo.',
                'item' => $item->fresh(),
            ], 409);
        }

        return response()->json($item->fresh());
    }

    public function destroy(Request $peticion, string $id): Response
    {
        $peticion->user()->items()->findOrFail($id)->delete();

        return response()->noContent();
    }

    public function restore(Request $peticion, string $id): JsonResponse
    {
        $item = $peticion->user()->items()->onlyTrashed()->findOrFail($id);
        $item->restore();

        return response()->json($item->fresh());
    }

    public function export(Request $peticion): JsonResponse
    {
        return response()->json([
            'formato' => 'vault-export-v1',
            'exportado_en' => now()->toIso8601String(),
            'items' => $peticion->user()->items()->withTrashed()->get(),
        ]);
    }
}
```

- [ ] **Step 5: Registrar las rutas dentro del grupo `auth`**

```php
Route::middleware('auth')->group(function () {
    Route::get('/vault/items', [ItemController::class, 'index']);
    Route::post('/vault/items', [ItemController::class, 'store']);
    Route::put('/vault/items/{id}', [ItemController::class, 'update']);
    Route::delete('/vault/items/{id}', [ItemController::class, 'destroy']);
    Route::post('/vault/items/{id}/restore', [ItemController::class, 'restore']);
    Route::get('/vault/export', [ItemController::class, 'export']);
});
```

- [ ] **Step 6: Ejecutar y comprobar que pasa**

Run: `php artisan test --filter=ItemTest`
Expected: PASS, 9 tests.

- [ ] **Step 7: Commit**

```bash
git add app/Http routes/api.php tests/Feature/ItemTest.php
git commit -m "Añade CRUD de ítems con papelera, export y concurrencia optimista"
```

---

### Task 11: Rotación de master password y ajustes de cuenta

**Files:**
- Create: `app/Http/Controllers/AccountController.php`
- Modify: `routes/api.php`
- Test: `tests/Feature/RotarMasterPasswordTest.php`

**Interfaces:**
- Consumes: modelo `User`, middleware `auth`.
- Produces:
  - `POST /api/account/master-password` `{auth_hash_actual, salt, kdf_algo, kdf_iterations, auth_hash, wrapped_vault_key, vault_key_iv}` → 204
  - `PUT /api/account/settings` `{auto_lock_seconds}` → 200

- [ ] **Step 1: Escribir el test que falla**

`tests/Feature/RotarMasterPasswordTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Hash;
use Tests\TestCase;

class RotarMasterPasswordTest extends TestCase
{
    use RefreshDatabase;

    private User $usuario;

    private string $actual = 'YWN0dWFs';

    protected function setUp(): void
    {
        parent::setUp();
        $this->usuario = User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'dmllam8=', 'kdf_algo' => 'pbkdf2-sha256', 'kdf_iterations' => 600000,
            'auth_hash' => Hash::make($this->actual),
            'wrapped_vault_key' => 'ZW52dWVsdGEtdmllamE=', 'vault_key_iv' => 'aXYtdmllam8=',
        ]);
    }

    private function payload(array $sobrescribir = []): array
    {
        return array_merge([
            'auth_hash_actual' => $this->actual,
            'salt' => base64_encode(random_bytes(16)),
            'kdf_algo' => 'pbkdf2-sha256',
            'kdf_iterations' => 600000,
            'auth_hash' => base64_encode(random_bytes(32)),
            'wrapped_vault_key' => base64_encode(random_bytes(48)),
            'vault_key_iv' => base64_encode(random_bytes(12)),
        ], $sobrescribir);
    }

    public function test_rota_salt_auth_hash_y_vault_key_envuelta(): void
    {
        $payload = $this->payload();

        $this->actingAs($this->usuario)
            ->postJson('/api/account/master-password', $payload)
            ->assertNoContent();

        $usuario = $this->usuario->fresh();
        $this->assertSame($payload['salt'], $usuario->salt);
        $this->assertSame($payload['wrapped_vault_key'], $usuario->wrapped_vault_key);
        $this->assertTrue(Hash::check($payload['auth_hash'], $usuario->auth_hash));
    }

    public function test_rechaza_si_la_master_password_actual_es_incorrecta(): void
    {
        $this->actingAs($this->usuario)
            ->postJson('/api/account/master-password', $this->payload(['auth_hash_actual' => 'bWFs']))
            ->assertStatus(422);

        $this->assertSame('dmllam8=', $this->usuario->fresh()->salt);
    }

    public function test_no_deja_el_usuario_a_medias_si_la_peticion_es_invalida(): void
    {
        $this->actingAs($this->usuario)
            ->postJson('/api/account/master-password', $this->payload(['wrapped_vault_key' => 'no base64 !!']))
            ->assertStatus(422);

        $usuario = $this->usuario->fresh();
        $this->assertSame('dmllam8=', $usuario->salt);
        $this->assertSame('ZW52dWVsdGEtdmllamE=', $usuario->wrapped_vault_key);
        $this->assertTrue(Hash::check($this->actual, $usuario->auth_hash));
    }

    public function test_exige_autenticacion(): void
    {
        $this->postJson('/api/account/master-password', $this->payload())->assertStatus(401);
    }

    public function test_permite_cambiar_el_tiempo_de_bloqueo(): void
    {
        $this->actingAs($this->usuario)
            ->putJson('/api/account/settings', ['auto_lock_seconds' => 900])
            ->assertOk();

        $this->assertSame(900, $this->usuario->fresh()->auto_lock_seconds);
    }

    public function test_rechaza_un_bloqueo_desmesurado(): void
    {
        $this->actingAs($this->usuario)
            ->putJson('/api/account/settings', ['auto_lock_seconds' => 999999])
            ->assertStatus(422);
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=RotarMasterPasswordTest`
Expected: FAIL — la ruta no existe.

- [ ] **Step 3: Implementar `AccountController`**

```php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Http\Response;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Hash;
use Illuminate\Validation\Rule;
use Illuminate\Validation\ValidationException;

class AccountController extends Controller
{
    public function rotarMasterPassword(Request $peticion): Response
    {
        // La validación ocurre entera antes de tocar nada: si algo falla,
        // el usuario queda exactamente como estaba.
        $datos = $peticion->validate([
            'auth_hash_actual' => ['required', 'string'],
            'salt' => ['required', 'string', 'base64'],
            'kdf_algo' => ['required', Rule::in(['pbkdf2-sha256'])],
            'kdf_iterations' => ['required', 'integer', 'min:600000'],
            'auth_hash' => ['required', 'string', 'base64'],
            'wrapped_vault_key' => ['required', 'string', 'base64'],
            'vault_key_iv' => ['required', 'string', 'base64'],
        ]);

        $usuario = $peticion->user();

        if (! Hash::check($datos['auth_hash_actual'], $usuario->auth_hash)) {
            throw ValidationException::withMessages([
                'auth_hash_actual' => 'La master password actual no es correcta.',
            ]);
        }

        // Todo o nada: dejar el auth hash nuevo con la Vault Key vieja envuelta
        // haría la bóveda inaccesible de forma permanente.
        DB::transaction(function () use ($usuario, $datos) {
            $usuario->update([
                'salt' => $datos['salt'],
                'kdf_algo' => $datos['kdf_algo'],
                'kdf_iterations' => $datos['kdf_iterations'],
                'auth_hash' => Hash::make($datos['auth_hash']),
                'wrapped_vault_key' => $datos['wrapped_vault_key'],
                'vault_key_iv' => $datos['vault_key_iv'],
            ]);
        });

        return response()->noContent();
    }

    public function settings(Request $peticion): JsonResponse
    {
        $datos = $peticion->validate([
            'auto_lock_seconds' => ['required', 'integer', 'min:60', 'max:3600'],
        ]);

        $peticion->user()->update($datos);

        return response()->json(['auto_lock_seconds' => $datos['auto_lock_seconds']]);
    }
}
```

- [ ] **Step 4: Registrar las rutas dentro del grupo `auth`**

```php
Route::post('/account/master-password', [AccountController::class, 'rotarMasterPassword']);
Route::put('/account/settings', [AccountController::class, 'settings']);
```

- [ ] **Step 5: Ejecutar y comprobar que pasa**

Run: `php artisan test --filter=RotarMasterPasswordTest`
Expected: PASS, 6 tests.

- [ ] **Step 6: Commit**

```bash
git add app/Http/Controllers/AccountController.php routes/api.php tests/Feature/RotarMasterPasswordTest.php
git commit -m "Añade rotación de master password atómica y ajustes de cuenta"
```

---

### Task 12: Cliente HTTP, stores y bloqueo automático

**Files:**
- Create: `resources/js/api/client.ts`
- Create: `resources/js/stores/session.ts`
- Create: `resources/js/stores/vault.ts`
- Create: `resources/js/composables/useAutoLock.ts`
- Test: `resources/js/stores/vault.test.ts`

**Interfaces:**
- Consumes: todo `@/crypto/*`, tipos de `@/types/item`.
- Produces:
  - `api.post/get/put/del(ruta, cuerpo?)` con CSRF y `Accept: application/json`
  - store `session`: `estado: 'anonimo' | 'semiautenticado' | 'desbloqueado' | 'bloqueado'`, `login()`, `desbloquear()`, `bloquear()`, `logout()`
  - store `vault`: `items: ItemDescifrado[]`, `cargar()`, `guardar()`, `eliminar()`, `restaurar()`, `buscar()`
  - `setVaultKey(k)`, `getVaultKey()`, `clearVaultKey()` — la clave vive aquí, fuera de Pinia

- [ ] **Step 1: Escribir el test que falla**

`resources/js/stores/vault.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { setVaultKey, getVaultKey, clearVaultKey } from './vault-key-holder'
import { generateVaultKey } from '@/crypto/vault-key'

describe('custodia de la Vault Key', () => {
  beforeEach(() => clearVaultKey())

  it('devuelve la clave que se le entregó', () => {
    const k = generateVaultKey()
    setVaultKey(k)
    expect(getVaultKey()).toBe(k)
  })

  it('lanza error si se pide la clave estando bloqueado', () => {
    expect(() => getVaultKey()).toThrow(/bloqueada/i)
  })

  it('olvida la clave al bloquear', () => {
    setVaultKey(generateVaultKey())
    clearVaultKey()
    expect(() => getVaultKey()).toThrow()
  })

  it('sobrescribe los bytes de la clave anterior al bloquear', () => {
    const k = generateVaultKey()
    const copia = new Uint8Array(k)
    setVaultKey(k)
    clearVaultKey()
    expect(Array.from(k)).not.toEqual(Array.from(copia))
    expect(k.every(b => b === 0)).toBe(true)
  })
})
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `npm test -- vault`
Expected: FAIL — no existe `./vault-key-holder`.

- [ ] **Step 3: Implementar `resources/js/stores/vault-key-holder.ts`**

```ts
/**
 * Custodia de la Vault Key.
 *
 * Vive en una variable de módulo, deliberadamente fuera de Pinia: así no aparece
 * en las devtools de Vue, no se serializa y no sobrevive a una recarga de página.
 */

let vaultKey: Uint8Array | null = null

export function setVaultKey(clave: Uint8Array): void {
  vaultKey = clave
}

export function getVaultKey(): Uint8Array {
  if (vaultKey === null) throw new Error('La bóveda está bloqueada.')

  return vaultKey
}

export function tieneVaultKey(): boolean {
  return vaultKey !== null
}

/** Sobrescribe los bytes antes de soltar la referencia. */
export function clearVaultKey(): void {
  if (vaultKey !== null) vaultKey.fill(0)
  vaultKey = null
}
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `npm test -- vault`
Expected: PASS.

- [ ] **Step 5: Implementar `resources/js/api/client.ts`**

Envoltorio sobre `fetch` con `credentials: 'same-origin'`, cabecera `X-XSRF-TOKEN` leída de la cookie, y que lance un error tipado con el `status` para que el store distinga un 409 de un 401.

- [ ] **Step 6: Implementar los stores `session` y `vault`**

`session` orquesta: `prelogin` → `deriveMasterKey` → `deriveAuthHash` → `login` → WebAuthn → `deriveWrappingKey` → `unwrapVaultKey` → `setVaultKey`. Al bloquear llama a `clearVaultKey()` y vacía `vault.items`.

`vault` mantiene `items: ItemDescifrado[]`, descifra al cargar, cifra al guardar, y ante un 409 recarga el ítem del servidor y avisa al usuario en lugar de sobrescribir.

- [ ] **Step 7: Implementar `useAutoLock`**

Escucha `mousemove`, `keydown`, `click` y `visibilitychange`. Emite un aviso 30 segundos antes del límite y bloquea al alcanzarlo. Se desmonta limpiamente.

- [ ] **Step 8: Verificar tipos**

Run: `npm run typecheck`
Expected: sin errores.

- [ ] **Step 9: Commit**

```bash
git add resources/js/api resources/js/stores resources/js/composables
git commit -m "Añade cliente HTTP, stores de sesión y bóveda, y bloqueo automático"
```

---

### Task 13: Pantallas de configuración y acceso

**Files:**
- Create: `resources/views/app.blade.php`
- Modify: `routes/web.php`
- Create: `resources/js/router.ts`
- Create: `resources/js/pages/Setup.vue`
- Create: `resources/js/pages/Login.vue`
- Create: `resources/js/pages/Desbloquear.vue`
- Modify: `resources/js/app.ts`

**Interfaces:**
- Consumes: stores `session`, `@/crypto/*`.
- Produces: rutas `/setup`, `/login`, `/` (bóveda), `/ajustes`. Guard que redirige a `/login` si el estado es `anonimo` y a `/desbloquear` si es `bloqueado`.

- [ ] **Step 1: Crear `resources/views/app.blade.php`**

Una única vista que monta la SPA. `routes/web.php` la devuelve para cualquier ruta que no empiece por `/api`.

```php
Route::get('/{cualquiera?}', fn () => view('app'))->where('cualquiera', '^(?!api).*$');
```

- [ ] **Step 2: Implementar `Setup.vue`**

Lee el `token` del query string. Pide master password dos veces, exige un mínimo de 12 caracteres, y muestra **de forma prominente** que no hay recuperación posible. Al enviar: genera salt, deriva, genera Vault Key, envuelve, `POST /api/setup`, y a continuación registra la primera passkey. Antes de dar por terminado, insiste en registrar una segunda.

- [ ] **Step 3: Implementar `Login.vue` y `Desbloquear.vue`**

`Login` ejecuta el flujo completo. `Desbloquear` solo pide la master password y rehace la wrapping key para desenvolver la Vault Key, sin volver a pasar por WebAuthn mientras la sesión siga viva.

- [ ] **Step 4: Verificar el arranque**

Run: `npm run build && php artisan serve`
Comprueba manualmente: `php artisan vault:init --email=tu@correo` y completa el flujo en el navegador.

- [ ] **Step 5: Commit**

```bash
git add resources routes/web.php
git commit -m "Añade las pantallas de configuración inicial, acceso y desbloqueo"
```

---

### Task 14: Pantalla de bóveda

**Files:**
- Create: `resources/js/pages/Boveda.vue`
- Create: `resources/js/components/ListaItems.vue`
- Create: `resources/js/components/BarraLateral.vue`
- Create: `resources/js/components/DetalleItem.vue`
- Create: `resources/js/composables/useClipboard.ts`
- Test: `resources/js/composables/useClipboard.test.ts`

**Interfaces:**
- Consumes: store `vault`, `CAMPOS_SECRETOS`.
- Produces: `copiarConBorrado(texto: string, segundos?: number): Promise<void>` y un `ref` con la cuenta atrás.

- [ ] **Step 1: Escribir el test de portapapeles que falla**

Usa temporizadores falsos de Vitest y un `navigator.clipboard` simulado. Verifica que se copia, que a los 25 segundos se escribe cadena vacía, y que copiar algo nuevo cancela el borrado pendiente del anterior.

- [ ] **Step 2: Implementar `useClipboard`**

- [ ] **Step 3: Ejecutar y comprobar que pasa**

Run: `npm test -- clipboard`

- [ ] **Step 4: Implementar la pantalla**

Barra lateral con filtros por tipo y carpeta y contador por grupo. Lista con búsqueda instantánea sobre `name`, `folder`, `fields.url` y `fields.service`; favoritos primero. Detalle con campos secretos ocultos tras un botón de revelar, botón de copiar en cada campo, `prod` resaltado en rojo y aviso de caducidad próxima.

Aplica el skill `frontend-design` para la dirección visual antes de escribir los estilos.

- [ ] **Step 5: Commit**

```bash
git add resources/js
git commit -m "Añade la pantalla de bóveda con búsqueda, filtros y copiado seguro"
```

---

### Task 15: Formularios y generador de contraseñas

**Files:**
- Create: `resources/js/components/FormularioItem.vue`
- Create: `resources/js/components/GeneradorPassword.vue`
- Create: `resources/js/lib/generador.ts`
- Test: `resources/js/lib/generador.test.ts`

**Interfaces:**
- Consumes: `CAMPOS_POR_TIPO`, store `vault`.
- Produces: `generarPassword(opciones: { longitud: number; simbolos: boolean; digitos: boolean; excluirAmbiguos: boolean }): string`

- [ ] **Step 1: Escribir el test que falla**

```ts
import { describe, it, expect } from 'vitest'
import { generarPassword } from './generador'

const base = { longitud: 20, simbolos: true, digitos: true, excluirAmbiguos: false }

describe('generarPassword', () => {
  it('respeta la longitud pedida', () => {
    expect(generarPassword({ ...base, longitud: 32 })).toHaveLength(32)
  })

  it('no repite el resultado entre llamadas', () => {
    const generadas = new Set(Array.from({ length: 100 }, () => generarPassword(base)))
    expect(generadas.size).toBe(100)
  })

  it('omite los símbolos cuando se desactivan', () => {
    for (let i = 0; i < 50; i++) {
      expect(generarPassword({ ...base, simbolos: false })).toMatch(/^[a-zA-Z0-9]+$/)
    }
  })

  it('omite los dígitos cuando se desactivan', () => {
    for (let i = 0; i < 50; i++) {
      expect(generarPassword({ ...base, digitos: false })).not.toMatch(/[0-9]/)
    }
  })

  it('excluye caracteres ambiguos cuando se pide', () => {
    for (let i = 0; i < 50; i++) {
      expect(generarPassword({ ...base, excluirAmbiguos: true })).not.toMatch(/[0O1lI|]/)
    }
  })

  it('incluye al menos un carácter de cada clase activada', () => {
    for (let i = 0; i < 50; i++) {
      const p = generarPassword({ ...base, longitud: 12 })
      expect(p).toMatch(/[a-z]/)
      expect(p).toMatch(/[A-Z]/)
      expect(p).toMatch(/[0-9]/)
    }
  })

  it('rechaza longitudes por debajo de 8', () => {
    expect(() => generarPassword({ ...base, longitud: 4 })).toThrow()
  })
})
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `npm test -- generador`

- [ ] **Step 3: Implementar `generarPassword`**

Selección uniforme con `crypto.getRandomValues` y rechazo de muestras sesgadas (descarta los valores que caen fuera del múltiplo mayor del tamaño del alfabeto). Garantiza al menos un carácter de cada clase activada y baraja el resultado con Fisher-Yates alimentado por la misma fuente.

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `npm test -- generador`
Expected: PASS, 7 tests.

- [ ] **Step 5: Implementar los formularios**

Campos según `CAMPOS_POR_TIPO`. `secure_text` usa `<textarea>` monoespaciada con copiado del bloque completo. Todos los campos llevan `autocomplete="off"` y quedan fuera de un `<form>` real para que el navegador no ofrezca guardarlos en su propio gestor.

- [ ] **Step 6: Commit**

```bash
git add resources/js
git commit -m "Añade formularios por tipo de ítem y generador de contraseñas"
```

---

### Task 16: Ajustes, papelera e importación

**Files:**
- Create: `resources/js/pages/Ajustes.vue`
- Create: `resources/js/pages/Papelera.vue`
- Create: `app/Console/Commands/VaultPurgar.php`
- Test: `tests/Feature/PurgarTest.php`

**Interfaces:**
- Consumes: `AccountController`, `ItemController`, `WebauthnController`.
- Produces: comando `vault:purgar` que destruye definitivamente los ítems con `deleted_at` de más de 30 días.

- [ ] **Step 1: Escribir el test que falla**

`tests/Feature/PurgarTest.php` — crea tres ítems: uno vivo, uno borrado hace 10 días y uno borrado hace 40. Ejecuta `vault:purgar` y comprueba que solo desaparece el tercero.

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=PurgarTest`

- [ ] **Step 3: Implementar el comando y programarlo**

```php
Item::onlyTrashed()->where('deleted_at', '<', now()->subDays(30))->forceDelete();
```

Programa en `routes/console.php`: `Schedule::command('vault:purgar')->daily();`

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `php artisan test --filter=PurgarTest`

- [ ] **Step 5: Implementar las pantallas**

`Ajustes`: gestión de passkeys (listar, añadir, borrar — con la última protegida), cambio de master password (que re-envuelve la Vault Key en cliente antes de enviar), tiempo de bloqueo, y export/import del fichero cifrado. El import solo acepta ficheros de la misma bóveda: si la Vault Key actual no descifra los ítems, avisa y no importa nada.

`Papelera`: lista de eliminados con restaurar y con los días restantes antes de la purga.

- [ ] **Step 6: Commit**

```bash
git add resources/js app/Console routes/console.php tests/Feature/PurgarTest.php
git commit -m "Añade ajustes, papelera con purga automática e importación"
```

---

### Task 17: Endurecimiento

**Files:**
- Create: `app/Http/Middleware/CabecerasDeSeguridad.php`
- Modify: `bootstrap/app.php`
- Create: `app/Providers/AppServiceProvider.php` (modificar el existente)
- Test: `tests/Feature/SeguridadTest.php`

**Interfaces:**
- Consumes: todas las rutas.
- Produces: middleware global de cabeceras y una comprobación de arranque que impide `APP_DEBUG=true` en producción.

- [ ] **Step 1: Escribir el test que falla**

`tests/Feature/SeguridadTest.php`:

```php
<?php

namespace Tests\Feature;

use App\Models\Item;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Str;
use Tests\TestCase;

class SeguridadTest extends TestCase
{
    use RefreshDatabase;

    public function test_las_cabeceras_de_seguridad_estan_presentes(): void
    {
        $respuesta = $this->get('/login');

        $respuesta->assertHeader('X-Content-Type-Options', 'nosniff');
        $respuesta->assertHeader('X-Frame-Options', 'DENY');
        $respuesta->assertHeader('X-Robots-Tag', 'noindex, nofollow');
        $respuesta->assertHeader('Referrer-Policy', 'no-referrer');
    }

    public function test_la_csp_no_permite_scripts_inline_ni_origenes_externos(): void
    {
        $csp = $this->get('/login')->headers->get('Content-Security-Policy');

        $this->assertStringContainsString("default-src 'self'", $csp);
        $this->assertStringNotContainsString('unsafe-inline', $csp);
        $this->assertStringNotContainsString('unsafe-eval', $csp);
        $this->assertStringNotContainsString('http://', $csp);
    }

    public function test_ningun_endpoint_de_la_boveda_devuelve_texto_en_claro(): void
    {
        $usuario = User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'c2FsdA==', 'kdf_algo' => 'pbkdf2-sha256', 'kdf_iterations' => 600000,
            'auth_hash' => 'hash', 'wrapped_vault_key' => 'ZW52', 'vault_key_iv' => 'aXY=',
        ]);

        Item::create([
            'id' => (string) Str::uuid(), 'user_id' => $usuario->id,
            'ciphertext' => base64_encode('CONTENIDO-CIFRADO'), 'iv' => base64_encode(random_bytes(12)),
        ]);

        foreach (['/api/vault/items', '/api/vault/export'] as $ruta) {
            $cuerpo = $this->actingAs($usuario)->getJson($ruta)->getContent();

            foreach (['password', 'username', 'fields', 'plaintext', 'name'] as $prohibido) {
                $this->assertStringNotContainsString(
                    "\"{$prohibido}\"", $cuerpo,
                    "El endpoint {$ruta} expone la clave '{$prohibido}'.",
                );
            }
        }
    }

    public function test_el_usuario_serializado_nunca_incluye_el_auth_hash(): void
    {
        $usuario = User::create([
            'email' => 'yo@ejemplo.com',
            'salt' => 'c2FsdA==', 'kdf_algo' => 'pbkdf2-sha256', 'kdf_iterations' => 600000,
            'auth_hash' => 'secreto-hash', 'wrapped_vault_key' => 'ZW52', 'vault_key_iv' => 'aXY=',
        ]);

        $this->assertArrayNotHasKey('auth_hash', $usuario->toArray());
        $this->assertStringNotContainsString('secreto-hash', json_encode($usuario));
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `php artisan test --filter=SeguridadTest`

- [ ] **Step 3: Implementar el middleware**

```php
<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class CabecerasDeSeguridad
{
    public function handle(Request $peticion, Closure $siguiente): Response
    {
        $respuesta = $siguiente($peticion);

        $respuesta->headers->set('Content-Security-Policy', implode('; ', [
            "default-src 'self'",
            "script-src 'self'",
            "style-src 'self'",
            "img-src 'self' data:",
            "font-src 'self'",
            "connect-src 'self'",
            "frame-ancestors 'none'",
            "base-uri 'self'",
            "form-action 'self'",
            "object-src 'none'",
        ]));

        $respuesta->headers->set('X-Content-Type-Options', 'nosniff');
        $respuesta->headers->set('X-Frame-Options', 'DENY');
        $respuesta->headers->set('X-Robots-Tag', 'noindex, nofollow');
        $respuesta->headers->set('Referrer-Policy', 'no-referrer');
        $respuesta->headers->set('Permissions-Policy', 'geolocation=(), camera=(), microphone=()');

        if ($peticion->secure()) {
            $respuesta->headers->set('Strict-Transport-Security', 'max-age=31536000; includeSubDomains; preload');
        }

        return $respuesta;
    }
}
```

Regístralo como middleware global en `bootstrap/app.php` con `$middleware->append(CabecerasDeSeguridad::class)`.

- [ ] **Step 4: Añadir la comprobación de arranque**

En `AppServiceProvider::boot()`:

```php
if ($this->app->environment('production') && config('app.debug')) {
    throw new \RuntimeException(
        'APP_DEBUG está activo en producción. Una traza de error aquí filtraría material sensible.'
    );
}
```

- [ ] **Step 5: Ajustar cookies de sesión**

En `config/session.php`: `'secure' => env('SESSION_SECURE_COOKIE', true)`, `'http_only' => true`, `'same_site' => 'strict'`, `'encrypt' => true`.

- [ ] **Step 6: Ejecutar toda la batería**

Run: `php artisan test && npm test`
Expected: todo en verde.

- [ ] **Step 7: Commit**

```bash
git add app bootstrap config tests/Feature/SeguridadTest.php
git commit -m "Endurece cabeceras, CSP y cookies; verifica que no se filtra plaintext"
```

---

### Task 18: Despliegue y backups

**Files:**
- Create: `docs/DESPLIEGUE.md`
- Create: `deploy/nginx.conf.ejemplo`
- Create: `deploy/backup.sh`
- Modify: `README.md`

**Interfaces:**
- Consumes: la aplicación completa.
- Produces: documentación operativa reproducible.

- [ ] **Step 1: Escribir `deploy/nginx.conf.ejemplo`**

Server block con TLS, `root` apuntando a `public/`, `try_files`, PHP-FPM por socket, denegación explícita de `.env`, `storage/`, `database/` y cualquier ruta con punto inicial.

- [ ] **Step 2: Escribir `deploy/backup.sh`**

```bash
#!/usr/bin/env bash
# Copia la bóveda cifrada fuera del VPS. El contenido ya es opaco:
# quien obtenga este fichero no tiene la master password.
set -euo pipefail

ORIGEN="/var/www/vault/database/database.sqlite"
DESTINO="/var/backups/vault"
FECHA="$(date +%Y%m%d-%H%M%S)"

mkdir -p "$DESTINO"

# .backup de sqlite3 es seguro con la aplicación en marcha; cp no lo es.
sqlite3 "$ORIGEN" ".backup '$DESTINO/vault-$FECHA.sqlite'"

find "$DESTINO" -name 'vault-*.sqlite' -mtime +30 -delete

echo "Copia creada: $DESTINO/vault-$FECHA.sqlite"
```

- [ ] **Step 3: Escribir `docs/DESPLIEGUE.md`**

Pasos numerados y verificables: clonar, `composer install --no-dev --optimize-autoloader`, `npm ci && npm run build`, `php artisan key:generate`, `php artisan migrate --force`, permisos de `storage/` y `database/`, `APP_DEBUG=false`, `APP_URL` con https, nginx, certbot, cron del backup, cron de `schedule:run`, y por último `php artisan vault:init`.

Incluye una sección **«Antes de confiar tus contraseñas a esto»** con tres comprobaciones obligatorias: registrar dos passkeys, verificar que una restauración de backup funciona, y confirmar que `https://tu-dominio/.env` devuelve 404.

- [ ] **Step 4: Reescribir `README.md`**

Qué es, la advertencia de irrecuperabilidad, cómo levantarlo en local, cómo ejecutar los tests, y enlace al spec y al despliegue.

- [ ] **Step 5: Verificación final completa**

```bash
php artisan test
npm test
npm run typecheck
npm run build
./vendor/bin/pint --test
```

Expected: todo en verde. Si Pint señala estilo, corrige con `./vendor/bin/pint` y vuelve a ejecutar.

- [ ] **Step 6: Commit**

```bash
git add docs deploy README.md
git commit -m "Añade documentación de despliegue, script de backup y verificación final"
```

---

## Notas de revisión del plan

Comprobado contra el spec sección por sección:

- Modelo criptográfico (spec §2) → Tasks 2, 3, 4
- Bootstrap (spec §3.1) → Tasks 6, 7, 13
- Login y desbloqueo (spec §3.2, §3.3) → Tasks 8, 9, 12, 13
- Modelo de datos (spec §4) → Task 5
- API (spec §5) → Tasks 7, 8, 9, 10, 11
- Frontend (spec §6) → Tasks 12, 13, 14, 15, 16
- Endurecimiento (spec §7) → Task 17
- Operación (spec §8) → Task 18
- Testing (spec §9) → distribuido en todas las tareas

**Dos riesgos conocidos, señalados donde tocan:**

1. La API de `web-auth/webauthn-lib` varía entre versiones mayores. La Task 9 fija la interfaz del servicio pero deja la implementación a la documentación de la versión que se instale, y ordena detenerse si hay conflicto de dependencias en lugar de forzarlo.
2. El vector de prueba de PBKDF2 en la Task 2 se confirma ejecutando Node antes de fijarlo, en vez de copiarlo de memoria.
